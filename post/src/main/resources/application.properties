spring.application.name=post

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/postDB
spring.datasource.username=localhost    # I'm not sure about the username
spring.datasource.password=qawsedrf

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080    # look into this
